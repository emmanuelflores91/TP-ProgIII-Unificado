<!-- filepath: src/views/delete.ejs -->
<%- include("partials/head") %>

<div class="crud-container">
  <div class="crud-header">
    <h1>🗑️ Eliminar Producto</h1>
    <a href="/admin" class="btn-admin btn-back">← Volver</a>
  </div>

  <div style="
    background: #2b1b17;
    border: 2px solid #c62828;
    border-radius: 8px;
    padding: 30px;
    text-align: center;
    margin-bottom: 30px;
  ">
    <div style="margin-bottom: 20px;">
      <% if (producto.ruta_img) { %>
        <img src="<%= producto.ruta_img %>" alt="<%= producto.nombre %>" 
             style="max-width: 150px; height: auto; border-radius: 4px; margin-bottom: 20px;">
      <% } %>
      
      <h2 style="color: #d4af37; margin: 0 0 10px 0;">
        <%= producto.nombre %>
      </h2>
      
      <p style="color: #a1887f; margin: 10px 0;">
        ID: #<%= producto.id %><br>
        Precio: $<%= producto.precio %><br>
        Stock: <%= producto.stock %>
      </p>
    </div>

    <div style="
      background: #c62828;
      color: white;
      padding: 20px;
      border-radius: 4px;
      margin-bottom: 30px;
    ">
      <strong>⚠️ ATENCIÓN:</strong><br>
      Esta acción <strong>NO SE PUEDE DESHACER</strong>.<br>
      ¿Estás seguro de que quieres eliminar este producto?
    </div>

    <form method="POST" action="/admin/eliminar" style="display: inline;">
      <input type="hidden" name="id" value="<%= producto.id %>">
      
      <button type="submit" class="btn-delete" style="
        padding: 15px 40px;
        font-size: 1.1em;
        cursor: pointer;
        border: none;
        border-radius: 4px;
        margin-right: 15px;
      ">
        🗑️ Sí, Eliminar
      </button>
      
      <a href="/admin" class="btn-submit" style="
        background: #555;
        text-decoration: none;
        padding: 15px 40px;
        font-size: 1.1em;
      ">
        ❌ Cancelar
      </a>
    </form>
  </div>
</div>

<%- include("partials/footer") %>